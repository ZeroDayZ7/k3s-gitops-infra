apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-ui
spec:
  template:
    spec:
      containers:
        - name: portfolio-ui
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
