apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-worker
spec:
  template:
    spec:
      containers:
        - name: go-worker
          env:
            - name: ENV
              value: "staging"
            - name: DB_HOST
              value: "mysql"
