apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-worker
spec:
  template:
    spec:
      containers:
        - name: go-worker
          resources:
            limits:
              cpu: "300m"
              memory: "64Mi"
            requests:
              cpu: "50m"
              memory: "32Mi"
