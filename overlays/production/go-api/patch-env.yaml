apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-api
spec:
  template:
    spec:
      containers:
        - name: go-api
          env:
            - name: ENV
              value: "staging"
            - name: DB_HOST
              value: "mysql-staging-internal"