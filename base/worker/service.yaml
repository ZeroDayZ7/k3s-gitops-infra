apiVersion: v1
kind: Service
metadata:
  name: go-worker
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: metrics
  selector:
    app: go-worker
